syntax = "proto3";

message User {}

service UserService {
  rpc CreateUser(User) returns (User);
  rpc ReadUser(User) returns (User);
  rpc UpdateUser(User) returns (User);
  rpc DeleteUser(User) returns (User);
  rpc ListAllUsers(User) returns (User);
}